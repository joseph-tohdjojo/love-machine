const atomSetup = require('./atom')
const bashSetup = require('./bash')
const gitSetup = require('./git')
const tmuxSetup = require('./tmux')
const vimSetup = require('./vim')

module.exports = {
  atomSetup,
  bashSetup,
  gitSetup,
  tmuxSetup,
  vimSetup,
}
